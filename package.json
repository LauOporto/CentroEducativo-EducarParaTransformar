{
  "name": "educar-para-transformar",
  "version": "0.1.0",
  "private": true,
  "description": "Campus virtual del colegio Educar para Transformar — backend (Node/Express/Prisma) + client (React 18 + Tailwind, en migración desde el frontend estático).",
  "scripts": {
    "dev": "pnpm --filter backend dev",
    "build": "pnpm --filter backend build",
    "backend:dev": "pnpm --filter backend dev",
    "client:dev": "pnpm --filter client dev",
    "client:build": "pnpm --filter client build"
  },
  "engines": {
    "node": ">=22.13",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@11.1.1"
}
